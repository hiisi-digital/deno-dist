# command-fixture

A fixture package for deno-dist's own tests. It is a command line tool and a
library at once, which is the shape that catches a distribution pointing its
command at the root export.
